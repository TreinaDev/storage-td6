<!DOCTYPE html>
<html>
  <head>
    <title>Storage</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-primary bg-primary">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <%= image_tag("logo_70px.png", size: "60") %>
        </a>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item h5"> <%= current_user.name %> </li>
              <li class="nav-item h5"> <%= link_to Supplier.model_name.human(count: 2), suppliers_path, class: "text-secondary" %> </li>
              <li class="nav-item h5"> <%= link_to Product.model_name.human(count: 2), product_entries_path, class: "text-secondary" %> </li>
              <li class="nav-item h5"> <%= link_to t('users.edit.title'), edit_user_path(current_user), class: "text-secondary" %> </li>
              <li class="nav-item h5"> <%= link_to t('reserved_items.nav_link'), reserved_items_path, class: "text-secondary" %> </li>
              <% if current_user.admin? %>
                <li class="nav-item h5"> <%= link_to t('users.nav_link'), users_path, class: "text-secondary" %> </li>
                <li class="nav-item h5"> <%= link_to t('warehouses.nav_link.new'), new_warehouse_path, class: "text-secondary" %> </li>
              <% else %>
                <li class="nav-item h5"> <%= link_to t('warehouses.nav_link.mine'), warehouse_path(current_user.warehouse), class: "text-secondary" %> </li>
              <% end %>
              <li class="nav-item h5"> <%= link_to t('devise.sessions.log_out'), destroy_user_session_path, method: :delete, class: "text-secondary" %> </li>
            <% else %>
              <li class="nav-item h5"> <%= link_to t('devise.sessions.log_in'), new_user_session_path, class: "text-secondary" %> </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div>
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div>
        <%= alert %>
      </div>
    <% end %>
    <%= yield %>

    <footer class="footer mt-auto bg-primary">
      <div class="container">
        <div class="d-inline-block">
          <p>Desenvolvido por:</p>
          <%= link_to 'Eric', "https://github.com/ericdaher", class: "text-secundary" %>
          <%= link_to 'Guilherme', "https://github.com/glmsilva", class: "text-secundary" %>
          <%= link_to 'Laryssa', "https://github.com/Laryssa-lfa", class: "text-secundary" %>
          <%= link_to 'Murilo', "https://github.com/cohako", class: "text-secundary" %>
          <%= link_to 'Rogerio', "https://github.com/rog404", class: "text-secundary" %>
        </div>
      </div>
    </footer>

  </body>
</html>
